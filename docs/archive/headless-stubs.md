## Headless Stubs Checklist

- [x] Task A — `tests/stubs.lua` — Create new file. Install no-op replacements for `love.graphics`, `love.keyboard`, and `love.filesystem` before any game module loads in headless mode. Explicit no-ops for every method the raycaster and scene code call: `setColor`, `rectangle`, `line`, `draw`, `setScissor`, `push`, `pop`, `translate`, `scale`, `setShader`, `setBlendMode`, `print`, `printf`, `clear`, `setDefaultFilter`. Catch-all `__index` metatable that returns a no-op for any unlisted key, except names starting with `new` which return a stub image table with `getWidth`/`getHeight`/`getDimensions` (→ 1, 1) and `setFilter` as no-ops. Special cases: `getDimensions` → `1280, 720`; `newShader` → `{ send = noop }`. Also stubs: `love.keyboard.isDown` → always `false`; `love.filesystem.getInfo` → always `nil`.

- [x] Task B — `tests/runner.lua` — Add `require("tests/stubs")` as the **first statement inside the `run` function** (before the `for _, path in ipairs(test_files)` loop on line ~78). Do **not** add it to `collect` — watch mode uses real graphics.

- [x] Task C — `core/lua/headless_input.lua` — Create new file. Port HeadlessInput from wip-3d. Class with `HeadlessInput.new()` (creates instance with empty `_down` and `_pressed` tables), `:press(action)` (sets both `_down[action]` and `_pressed[action]` = true; auto-cleared after one `:update()` call), `:hold(action)` (sets `_down[action]` = true, `_pressed[action]` only on first frame), `:release(action)` (clears both tables for that action), `:update()` (clears all `_pressed` flags — call before scene:update()), `:is_down(action)` → `_down[action] == true`, `:pressed(action)` → `_pressed[action] == true`.
