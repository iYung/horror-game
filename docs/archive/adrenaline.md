## Adrenaline Shot Checklist

- [x] Task A — `game/data/items.lua` — add `adrenaline` definition entry to `definitions` (id, name, value=2, boost_duration=5, boost_mult=1.75, cooldown_duration=20) and a `make_adrenaline` factory function that initialises state fields (`boosting=false`, `on_cooldown=false`, `boost_timer=nil`, `cooldown_timer=nil`, `speed_mult=1.0`), defines `use_fn` (no-op if boosting or on_cooldown; otherwise set boosting=true, speed_mult=1.75, start boost_timer), defines `update(dt)` (tick boost_timer while boosting; on expiry clear boost and start cooldown_timer; tick cooldown_timer while on_cooldown; on expiry clear cooldown), and registers the factory in the `factories` table.

- [x] Task B — `game/entities/player.lua` — in `Player:update`, before computing `dx`/`dy`, read `self.inventory:active()` and apply its `speed_mult` field (if present) as a multiplier on `SPEED_GU * dt` so boosted movement scales correctly without touching any other code path.

- [x] Task C — `game/scenes/run_scene.lua` — in the `update` loop, add adrenaline item update calls: iterate `self.player.inventory.slots` (slots 1–5) and call `item.update(dt)` for any slot whose item has an `update` field (this is needed because adrenaline lives in inventory, not on the ground, and the existing ground-item update loop will not reach it). Also add `adrenaline` to the color lookup in `build_sprites`: `or id == "adrenaline" and {0.2, 1.0, 0.4, 1}` (bright green), inserted before the fallback yellow color.

- [x] Task D — `game/ui/hud.lua` — after drawing the item name in a slot, add an adrenaline status overlay: when the slot's item has `id == "adrenaline"` and the slot is the active slot, draw a small status label inside the slot below the item name using `ITEM_FONT`; show amber `BOOST` text while `item.boosting` is true, or dim-red `CD Xs` (remaining cooldown seconds, rounded up) while `item.on_cooldown` is true using `item.cooldown_timer` to read time remaining; show nothing when the item is ready. Use the existing `COLOR_AMBER` constant for the boost state.

- [x] Task E — `game/world/item_spawner.lua` — following the compass block pattern, add a new block that spawns 1–2 adrenaline shots when `budget >= 2`: call `pick_random(walkable, math.random(1, 2), reserved)`, create each item with `Items.new("adrenaline")`, insert into `ground` at cell-centre world coordinates, and mark chosen cells reserved.

- [x] Task F — `test/run_test.lua` — add two new `it()` tests inside the existing `describe("Simulation", ...)` block: (1) a smoke test `"adrenaline item can be held without errors"` that runs 10 frames with `loadout_item = Items.new("adrenaline")` and asserts state is non-nil; (2) an activation test `"adrenaline use_fn applies speed multiplier"` that gives the player an adrenaline loadout item, calls `item.use_fn()` directly, and asserts `item.speed_mult > 1.0` and `item.boosting == true`. After writing the tests, run the full suite with `love . -- --headless` from `/root/horror-game` and confirm it exits 0.
