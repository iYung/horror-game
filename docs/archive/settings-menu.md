## Settings Menu Checklist

- [x] Task 1 — `assets/menu_btn.png`, `assets/menu_btn_selected.png`, `assets/settings_background.png` — Copy the three asset files from `/root/wip/assets/` into `/root/horror-game/assets/` using a file-copy operation. These images are required by `settings_menu.lua` at load time via `love.graphics.newImage`.

- [x] Task 2 — `game/settings_state.lua` — Create this new module. It must export a `SettingsState` class with a constructor `SettingsState.new()` that returns `{ fullscreen = false }` and a method `toggle_fullscreen()` that flips `self.fullscreen` and calls `love.window.setFullscreen(self.fullscreen)`. Return the class at the bottom of the file.

- [x] Task 3 — `game/scenes/settings_menu.lua` — Create this new module as a plain Lua table (not a Scene subclass). Requires `game/settings_state` but receives an already-constructed `SettingsState` instance via `SettingsMenu.new(settings_state)`. Layout constants: `BTN_W=300`, `BTN_H=54`, centred horizontally in 1280-wide window, rows spaced 74 px apart. Three items in order: `"Fullscreen / Window"` (label reads `"Window"` when `settings_state.fullscreen` is true, `"Fullscreen"` when false), `"Exit Settings"`, `"Leave Game"`. Public fields: `is_open = false`. Methods: `open(opaque)` — sets `is_open = true`, stores `opaque` flag, snapshots `love.keyboard.isDown` state for all navigation/confirm keys to prevent bleed-through on the frame of opening; `close()` — sets `is_open = false`; `update(dt)` — reads `love.keyboard.isDown` directly with `_prev_*` edge-trigger flags for `up`, `down`, `e`, `f`, `return`, `space`, `escape`; navigates the selected index with edge-wrap; confirms the selected item on `E`/`F`/`return`/`space` (item 1 calls `settings_state:toggle_fullscreen()`, item 2 calls `self:close()`, item 3 calls `love.event.quit()`); closes on `escape`; `draw()` — if opaque draws `settings_background.png` image filling the screen, else draws a `0,0,0,0.55` alpha rect over the full screen; then draws `menu_btn_selected.png` for the selected row and `menu_btn.png` for unselected rows at the computed positions, with item label text centred on each button; `keypressed(key)` — no-op stub that returns `false` (reserved for future capture flows). Load `menu_btn.png`, `menu_btn_selected.png`, and `settings_background.png` once at module level using `love.graphics.newImage`.

- [x] Task 4 — `main.lua` — Patch the normal game `else` branch only (leave `headless` and `watch` branches untouched). In `love.load()`, after `manager:switch(PlanningScene.new())`, require `game/settings_state` and `game/scenes/settings_menu`, construct `ss = SettingsState.new()` and `settings_menu = SettingsMenu.new(ss)` as upvalues accessible to the other love callbacks. Replace `love.update(dt)` so that if `settings_menu.is_open` it calls `settings_menu:update(dt)` and skips `manager:update(dt)`, otherwise calls `manager:update(dt)` as before. Replace `love.draw()` to always call `manager:draw()` and then, if `settings_menu.is_open`, call `settings_menu:draw()` on top. Replace `love.keypressed(key)` with the routing logic from the design doc: first give the open menu first crack via `settings_menu:keypressed(key)` (return if it returns truthy); then handle `escape` by toggling the menu open/closed when `manager.current.esc_opens_settings` is set; finally fall through to `manager.current:keypressed(key)` as before.

- [x] Task 5 — `game/scenes/planning_scene.lua` — In `PlanningScene.new()`, after the existing field assignments, add `self.esc_opens_settings = true` and `self.esc_settings_opaque = true`. No other changes — PlanningScene has no existing Escape handler to remove.

- [x] Task 6 — `game/scenes/run_scene.lua` — In `RunScene.new()`, add `self.esc_opens_settings = true`. Remove the entire existing `keypressed` method (lines 270–276 in the current file) which handles `escape` by switching to `PlanningScene` — this logic is now owned by `main.lua`'s `love.keypressed` router. If `keypressed` has no other cases after removing the escape block, delete the method entirely.

- [x] Task 7 — `tests/test_settings_state.lua` — Create this test file using the project's runner framework (`describe` / `it` / `assert.*` globals). Before requiring `game/settings_state`, stub `love.window` as `love.window = { setFullscreen = function(v) end }` at the top of the file so the module loads in headless mode. Tests to include: `"SettingsState.new() initialises fullscreen to false"` — construct a new instance and assert `assert.is_false(state.fullscreen)`; `"toggle_fullscreen flips fullscreen to true"` — call `toggle_fullscreen()` once and assert `assert.is_true(state.fullscreen)`; `"toggle_fullscreen flips back to false"` — call again and assert `assert.is_false(state.fullscreen)`; `"toggle_fullscreen calls love.window.setFullscreen with correct value"` — capture the argument passed to the stub and assert it matches the new value of `state.fullscreen` after each call.

- [x] Task 8 — `tests/test_settings_menu.lua` — Create this test file using the same runner framework. At the top of the file, before requiring any game module, stub the Love2D APIs that `settings_menu.lua` calls at module load or test time: `love.graphics.newImage` returns a table stub with a `getDimensions` method returning `300, 54`; `love.window.setFullscreen` is a no-op; `love.event.quit` captures the last argument so tests can assert it was called; `love.keyboard.isDown` returns `false` by default (tests can override per-key). Also stub `love.graphics.draw`, `love.graphics.setColor`, `love.graphics.rectangle`, `love.graphics.print`, `love.graphics.getFont`, `love.graphics.setFont`, `love.graphics.newFont` as no-ops so `draw()` can be called without errors. Require `game/settings_state` and `game/scenes/settings_menu` after the stubs are in place. Tests to include: `"is_open starts false"` — `assert.is_false(menu.is_open)`; `"open sets is_open to true"` — call `menu:open(false)`, assert `assert.is_true(menu.is_open)`; `"close sets is_open to false"` — open then close, assert `assert.is_false(menu.is_open)`; `"escape key in update closes the menu"` — open the menu, override `love.keyboard.isDown` to return `true` only for `"escape"`, call `menu:update(0)`, assert `assert.is_false(menu.is_open)`; `"selecting Fullscreen/Window item calls toggle_fullscreen"` — open the menu, set `_selected = 1`, simulate confirm key press via `love.keyboard.isDown`, call `update(0)`, assert `settings_state.fullscreen` flipped; `"selecting Leave Game calls love.event.quit"` — open the menu, set `_selected = 3`, simulate confirm key, call `update(0)`, assert `quit` stub was called; `"keypressed returns false"` — assert `assert.is_false(menu:keypressed("escape"))`.
