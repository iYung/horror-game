## Tracker Checklist

- [x] Task 1 — `game/data/items.lua` — Add `tracker` definition and factory. In the `definitions` table add `tracker = { id = "tracker", name = "Tracker", value = 1 }`. Add a `make_tracker` function that copies `definitions.tracker` into a new table and returns it (mirrors `make_compass`). Register it in the `factories` table as `tracker = make_tracker`. Update the module comment at the top of the file to document the new item.

- [x] Task 2 — `game/world/item_spawner.lua` — Spawn trackers when `budget >= 1`. Insert a new block between the flashlight block (which already fires at `budget >= 1`) and the compass block (at `budget >= 3`): draw `math.random(1, 2)` cells via `pick_random(walkable, tr_count, reserved)`, call `cell_centre` on each, and `table.insert` a ground item with `Items.new("tracker")`. Mark each chosen cell in `reserved` with `cell.col * 1000 + cell.row`. Update the module comment at the top of the file (lines 3–6) to document tracker spawn behaviour.

- [x] Task 3 — `game/scenes/run_scene.lua` — Give the tracker a distinct orange-red billboard color and pass `self.monster` to `HUD.new`. In `build_sprites` (around line 188–189), extend the color-dispatch chain to add `or id == "tracker" and {1, 0.4, 0.1, 1}` after the existing `compass` entry. Update the `HUD.new` call (line 72) from `HUD.new(self.player.inventory, self.extraction, self.player)` to `HUD.new(self.player.inventory, self.extraction, self.player, self.monster)`. (NOTE: Task 4 depends on `HUD.new` accepting a fourth argument, so Task 3 and Task 4 must be completed in order: Task 3 first, Task 4 second.)

- [x] Task 4 — `game/ui/hud.lua` — Extend `HUD.new` and `HUD:draw` to render the tracker arrow. Change the `HUD.new` signature (line 41) from `function HUD.new(inventory, extraction, player)` to `function HUD.new(inventory, extraction, player, monster)` and store `self.monster = monster`. After the existing Compass arrow block in `HUD:draw`, add a Tracker arrow block: guard with `if self.player and self.monster`, get the active item, and when `item.id == "tracker"` compute `dx = self.monster.x - pc.x` and `dy = self.monster.y - pc.y` (both already in pixel space), derive `world_angle = math.atan2(dy, dx)` and `relative_angle = world_angle - self.player.angle`, then `love.graphics.push()`, `translate(640, 680)`, `rotate(relative_angle)`, `setColor(1, 0.15, 0.15, 0.85)`, `polygon("fill", 0, -18, -10, 10, 10, 10)`, `love.graphics.pop()`. (Depends on Task 3 having updated the `HUD.new` call site so the fourth argument is passed.)

- [x] Task 5 — `test/run_test.lua` — Add a smoke test for the tracker item mirroring the existing `"compass item can be held without errors"` test. Call `make_config` with `budget = 1`, `map_id = "forest"`, `monster_traits = {}`, and `loadout_item = Items.new("tracker")`. Construct `Simulation.new(run_config)` and call `sim:step(1 / 60)` 10 times in a loop. Assert that the final `state` is not nil. Place the new `it()` block directly after the compass smoke test. Run the full suite (`love . -- --headless`) after adding the test and confirm exit code 0.
